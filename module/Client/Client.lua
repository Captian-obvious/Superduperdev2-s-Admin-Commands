client = {
    Prefix = ';',
    Connectors = {},
    Runners = {},
}
server = require(script.Parent.Parent.Server.Server)
remote = server.Remote

function client:Initialize(plr)
    if plr~=nil then
        
    end
end

return client
